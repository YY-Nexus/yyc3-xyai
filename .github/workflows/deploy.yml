name: YYC³ AI小语 - 部署工作流

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      environment:
        description: '部署环境'
        required: true
        default: 'production'
        type: choice
        options:
        - production
        - staging
        - development

jobs:
  test:
    name: 测试和验证
    runs-on: ubuntu-latest
    outputs:
      should-deploy: ${{ steps.changes.outputs.should-deploy }}

    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: 设置 Bun
      uses: oven-sh/setup-bun@v1
      with:
        bun-version: latest

    - name: 安装依赖
      run: bun install --frozen-lockfile

    - name: 检查变更
      id: changes
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          echo "should-deploy=true" >> $GITHUB_OUTPUT
        elif [ "${{ github.ref }}" = "refs/heads/main" ]; then
          echo "should-deploy=true" >> $GITHUB_OUTPUT
        else
          echo "should-deploy=false" >> $GITHUB_OUTPUT
        fi

    - name: 类型检查
      run: bun run tsc --noEmit

    - name: 代码检查
      run: bun run lint

    - name: 运行测试
      run: bun run test:ci

    - name: 构建验证
      run: bun run build

  security:
    name: 安全检查
    runs-on: ubuntu-latest
    needs: test
    if: needs.test.outputs.should-deploy == 'true'

    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 Bun
      uses: oven-sh/setup-bun@v1
      with:
        bun-version: latest

    - name: 安装依赖
      run: bun install --frozen-lockfile

    - name: 运行安全审计
      run: bun audit

    - name: 检查敏感信息
      uses: trufflesecurity/trufflehog@main
      with:
        path: ./
        base: main
        head: HEAD

  build:
    name: 构建应用
    runs-on: ubuntu-latest
    needs: [test, security]
    if: needs.test.outputs.should-deploy == 'true'

    strategy:
      matrix:
        environment:
          - production
          - staging

    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 Bun
      uses: oven-sh/setup-bun@v1
      with:
        bun-version: latest

    - name: 设置 Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'bun'

    - name: 安装依赖
      run: bun install --frozen-lockfile

    - name: 加载环境变量
      run: |
        if [ "${{ matrix.environment }}" = "production" ]; then
          echo "NEXT_PUBLIC_ENVIRONMENT=production" >> .env.local
          echo "NEXT_PUBLIC_API_URL=https://api.yyc3.ai" >> .env.local
          echo "ANALYTICS_ID=GA_MEASUREMENT_ID" >> .env.local
        else
          echo "NEXT_PUBLIC_ENVIRONMENT=staging" >> .env.local
          echo "NEXT_PUBLIC_API_URL=https://staging-api.yyc3.ai" >> .env.local
          echo "ANALYTICS_ID=GA_STAGING_ID" >> .env.local
        fi

    - name: 构建应用
      run: bun run build
      env:
        NEXT_PUBLIC_ENVIRONMENT: ${{ matrix.environment }}
        NODE_ENV: production

    - name: 构建产物检查
      run: |
        ls -la .next/
        du -sh .next/

    - name: 上传构建产物
      uses: actions/upload-artifact@v3
      with:
        name: build-${{ matrix.environment }}
        path: .next/
        retention-days: 7

  deploy-vercel:
    name: 部署到 Vercel
    runs-on: ubuntu-latest
    needs: [test, security, build]
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    environment: production

    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 下载构建产物
      uses: actions/download-artifact@v3
      with:
        name: build-production
        path: .next/

    - name: 部署到 Vercel
      uses: amondnet/vercel-action@v25
      with:
        vercel-token: ${{ secrets.VERCEL_TOKEN }}
        vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
        vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
        vercel-args: '--prod'
        working-directory: ./

  deploy-docker:
    name: 构建和部署 Docker
    runs-on: ubuntu-latest
    needs: [test, security, build]
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    environment: production

    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: 登录 Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: 提取元数据
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: yyc3/yyc3-ai-xiaoyu
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: 构建和推送 Docker 镜像
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: 部署到服务器
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USERNAME }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        script: |
          cd /opt/yyc3-ai
          docker-compose pull
          docker-compose up -d
          docker system prune -f

  health-check:
    name: 健康检查
    runs-on: ubuntu-latest
    needs: [deploy-vercel, deploy-docker]
    if: always() && (needs.deploy-vercel.result == 'success' || needs.deploy-docker.result == 'success')

    steps:
    - name: 等待部署完成
      run: sleep 60

    - name: 检查生产环境
      run: |
        curl -f https://yyc3.app || exit 1
        curl -f https://yyc3.app/zh || exit 1
        curl -f https://yyc3.app/en || exit 1
        echo "生产环境部署成功！"

    - name: 检查 API 端点
      run: |
        curl -f https://api.yyc3.ai/health || exit 1
        echo "API 服务正常！"

  notify:
    name: 通知部署结果
    runs-on: ubuntu-latest
    needs: [health-check]
    if: always()

    steps:
    - name: 发送成功通知
      if: needs.health-check.result == 'success'
      run: |
        echo "✅ YYC³ AI小语部署成功！"
        echo "🌐 生产环境: https://yyc3.app"
        echo "📊 部署时间: $(date)"

    - name: 发送失败通知
      if: needs.health-check.result == 'failure'
      run: |
        echo "❌ YYC³ AI小语部署失败！"
        echo "🔍 请检查部署日志"
        echo "⏰ 失败时间: $(date)"
        exit 1